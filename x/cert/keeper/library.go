package keeper

// Library keeper logic has been removed. The library concept had no tx/query/proto/CLI
// surface and no downstream dependencies. It is cleaned up via the Migrate2to3 migration
// which deletes obsolete library store entries from existing chain state.
